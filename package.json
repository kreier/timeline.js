{
  "name": "timeline.js",
  "version": "0.1.0",
  "description": "Browser-based generator for the kreier/timeline PDF (human history timeline), using the original project's CSV data, fonts and images.",
  "type": "module",
  "scripts": {
    "sync-assets": "node scripts/sync-assets.js",
    "predev": "node scripts/sync-assets.js",
    "dev": "vite",
    "prebuild": "node scripts/sync-assets.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "papaparse": "^5.4.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "vite": "^6.4.3"
  },
  "license": "Apache-2.0",
  "allowScripts": {
    "esbuild": true,
    "fsevents": true
  }
}
